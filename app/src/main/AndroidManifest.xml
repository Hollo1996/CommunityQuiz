<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.bme.communityquiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CommunityQuiz">
        <activity android:name=".ui.answer_question.AnswerQuestionActivity"></activity>
        <activity android:name=".ui.create_question.CreateQuestionActivity" />
        <activity android:name=".ui.score.ScoreActivity" />
        <activity android:name=".ui.choose_category.ChooseCategoryActivity" />
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>